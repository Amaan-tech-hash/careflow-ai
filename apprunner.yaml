version: 1.0
runtime: nodejs16

build:
  commands:
    build:
      - npm install
      - npm run build

run:
  command: npx serve -s build -l 8080
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: production